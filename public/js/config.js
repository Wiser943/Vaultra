// VAULTRA runtime frontend config.
// After deploying the backend to Render, set this to your backend origin, for example:
// window.VAULTRA_API_BASE = 'https://vaultra-zacl.onrender.com';
// If you use the Netlify /api/* proxy in netlify.toml, leave it blank.
window.VAULTRA_API_BASE = window.VAULTRA_API_BASE || '';
